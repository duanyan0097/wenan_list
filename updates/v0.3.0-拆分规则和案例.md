# v0.3.0 拆分规则和案例

## 更新时间

2026-05-25

## 本次调整

- 删除旧的合并文档。
- 新增 `rules/` 目录，专门保存写作规则。
- 新增 `cases/` 目录，每个问题单独保存一个文件。
- 修正 `README.md` 和 `CHANGELOG.md` 中文显示问题。

## 为什么这样改

规则、案例和更新说明放在同一个文件里，后续会越来越难查。

拆分以后，每次新增一个问题，就只需要在 `cases/` 新建一个独立文件。这样 GitHub Desktop 的历史记录也会更清楚。
